const person = {
    name = "Henrique",
    idade = 20,
    altura = 190,
    isAdmin = true
}

console.log(person.name) //unica propriedade do objeto
console.log(`${person.name} tem ${person.idade} anos`) //interpolando no objeto